//Q. Find area of a rectangle.
//A.
		#include <stdio.h>
			int main() {
				int l, b;
				printf("Please enter the length and the breadth: ");
				scanf("%d%d", &l, &b);	
				printf("The area of the rectangle is: %d\n", l*b);
					}
