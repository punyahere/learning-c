//Q. Add two numbers together.
//A. 
	#include <stdio.h>
		int main() {
			int num1, num2;
			printf("Enter two numbers: ");
			scanf("%d%d", &num1, &num2);
			printf("The sum of both the numbers is: %d\n", num1+num2);
				}
