//Q.Program to convert length in cm to km.
//A.
	#include <stdio.h>
		int main() {
			int length;
			printf("Please enter the length: ");
			scanf("%d", &length);
			printf("The length in km is %d", length*1000);
				}
