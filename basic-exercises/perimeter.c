//Q. Find the perimeter of a rectangle.
//A. 
	#include <stdio.h>
		int main() {
			int l, b;
			printf("Enter the length and the breadth of the rectangle: ");
			scanf("%d%d", &l, &b);
			printf("The perimeter of the rectangle is: %d\n", l*b*2);
				}	
